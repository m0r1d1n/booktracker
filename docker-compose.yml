services:
  booktracker:
    build: .
    container_name: booktracker
    ports:
      - "8000:8000"
    volumes:
      - booktracker_data:/app/data
    restart: unless-stopped

volumes:
  booktracker_data:
